graph_title Database statement rate
graph_order reads writes
graph_vlabel statements / ${graph_period}
graph_category database
reads.label reads
reads.type DERIVE
reads.draw LINE1
reads.min 0
reads.max 10000
writes.label writes
writes.type DERIVE
writes.draw LINE1
reads.min 0
reads.max 10000
