<?xml version="1.0" encoding="ISO-8859-1"?>
<document>
  <properties>
    <title>Overview of web components</title>
  </properties>
  <body>
    <section name="Web components">
      <subsection name="ServletFileSystemProvider">
        <p>This is an implementation of the FileSystemProvider that delegates it's functionality
        to the <code>javax.servlet.ServletContext.getResource(String)</code> calls. 
        Stat operations, directory existence checking and listings are supported only if there 
        are listing files available. The ObjectLedge Maven plugin is able to generate these files 
        automatically.</p>
        
        <p class="more"><a href="ServletFileSystemProvider.html">more</a></p>
      </subsection>
      
      <subsection name="Upload">
        <p>The Upload component is the implementation of the pipeline processing valve,
        that is run at very early stage of the pipeline processing. When it discovers the
        "multipart/form-data" it starts to process the request storing the uploaded items into 
        the context.
        </p>
        <p class="more"><a href="UploadValve.html">more</a></p>
      </subsection>
      <subsection name="Web configurator">
        <p>The simple component providing common configuration for many web components i.e.
         the default locale, default encoding, mvc path info tokens...
        </p>
        <p class="more"><a href="WebConfigurator.html">more</a></p>
      </subsection>
      <subsection name="Request parameters loader valve">
        <p>The pipeline processing valve that read the parameters from request parameters and 
        from parsed url path and store it as a parameters container in thread context.
        </p>
        <p class="more"><a href="RequestParameters.html">more</a></p>
      </subsection>
      <subsection name="Locale loader valve">
        <p>The pipeline processing valve responsible for loading the user prefered locale and encoding.
        </p>
        <p class="more"><a href="LocaleLoaderValve.html">more</a></p>
      </subsection>
      <subsection name="Web I18n tool factory">
        <p>The pipeline processing valve responsible for loading the user prefered locale and encoding.
        </p>
        <p class="more"><a href="WebI18nToolFactory.html">more</a></p>
      </subsection>
      <subsection name="Exception redirector valve">
        <p>The pipeline processing valve which redirect to appropriate view if the exception occured 
        during pipeline processing.
        </p>
        <p class="more"><a href="ExceptionRedirectorValve.html">more</a></p>
      </subsection>
      <subsection name="PolicySystem">
        <p>The simple component to checks user permission to execute the action and
	     access the view.</p>
        <p class="more"><a href="PolicySystem.html">more</a></p>
      </subsection>
    </section>
  </body>
</document>
  
